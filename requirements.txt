# Project-wide Python runtime dependencies.
# This aggregate file includes the runtime dependencies of both Python services.
# Install it from the repository root.
-r reference-service/requirements.txt
-r evaluation-service/requirements.txt
